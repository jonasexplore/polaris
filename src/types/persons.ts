export type Person = {
  id?: string;
  document: string;
  name: string;
  email: string;
  birthDate?: string;
  phone?: string;
  address?: string;
  city?: string;
  state?: string;
  country?: string;
  zip?: string;
};
